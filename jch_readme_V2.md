# ⚖️ 스코어링 단계 (⑦) 로직 정리 — jch

> **이 문서는** V2 파이프라인 중 스코어링 단계(⑦)의 **구현 로직·설계 근거·진단 결과**를 정리한 노트. 단일 강의 end-to-end 검증(2026-02-02 오전)에서 발견한 **C5 0.0점 원인 규명**과 후속 고도화 후보를 포함한다.

NLP 과제 1 · pipeline/scoring/jch/v2 브랜치

---

## 📌 파이프라인 내 위치

스코어링은 분석 ⑥(LLM 채점)의 결과를 받아 **규칙 기반으로** 항목 가중 종합점수를 산출한다. LLM 호출이 없는 유일한 단계다.

| 항목 | 내용 |
|---|---|
| 진입점 | `python -m scripts.run_score_local --analysis outputs/processed/analysis.jsonl` |
| 입력 | `analysis.jsonl` — 체크리스트 18항목 채점 결과 (분석 ⑥ 산출물) |
| 출력 | `scores.json` — 강의별 `category_scores`(5개) · `total_score` · `items`(18항목) · `summary` |
| **LLM 호출** | **없음 (파이프라인 중 API 비용 0인 유일한 단계)** |

---

## 🔢 점수 계산 공식 (scoring.py)

### 정규화

항목 점수 1~5를 0~100% 선형 변환한다.

```python
norm = (score - 1) / (5 - 1) * 100   # 1점 → 0.0%, 5점 → 100.0%
```

### 가중치

PDF 가중치(높음/중간/낮음)를 숫자로 매핑한다.

```python
WEIGHT_VALUE = {"high": 3, "mid": 2, "low": 1}
```

### 카테고리 점수 · 종합 점수

```python
# 카테고리 점수 — 카테고리 내 항목 가중 평균
category_score[C] = Σ(norm × w) / Σ(w)   for items in C

# 종합 점수 — 18항목 전체 항목가중 평균 (카테고리 단위 선가중 아님)
total_score = Σ(norm × w) / Σ(w)          for all 18 items
```

> 종합은 **카테고리 평균의 평균이 아니라** 항목 18개를 직접 가중한다. 항목 수가 카테고리마다 다르고(C1=3, C2=5, C3=4, C4=3, C5=3) 가중치도 다르므로 두 방식은 결과가 다르다.

### N/A 처리

`score=None` 항목은 분자·분모 모두에서 제외한다 — 해당 항목이 없어도 나머지 항목으로 점수가 계산된다.

---

## 🏷️ 품질 기준 매핑 — PDF ver.2.0 ↔ 코드 item_key

**18항목 전체 일치 확인 (18/18).** 오류 대응(`C4_error`)은 C4 소속이며 C5가 아님(혼동 주의).

| 카테고리 | PDF 항목명 | PDF 가중치 | 코드 item_key | eval_type | 일치 |
|---|---|---|---|---|---|
| C1 언어 표현 품질 | 불필요한 반복 표현 | **높음** | `C1_repetition` | metric | ✅ |
| C1 | 발화 완결성 | 중간 | `C1_completeness` | global | ✅ |
| C1 | 언어 일관성 | 중간 | `C1_consistency` | global | ✅ |
| C2 강의 도입·구조 | 학습 목표 안내 | **높음** | `C2_objective` | intro | ✅ |
| C2 | 전날 복습 연계 | **높음** | `C2_review` | intro | ✅ |
| C2 | 설명 순서 | 중간 | `C2_order` | global | ✅ |
| C2 | 핵심 내용 강조 | 중간 | `C2_emphasis` | local | ✅ |
| C2 | 마무리 요약 | 낮음 | `C2_summary` | outro | ✅ |
| C3 개념 설명 명확성 | 개념 정의 | **높음** | `C3_definition` | local | ✅ |
| C3 | 비유 및 예시 활용 | **높음** | `C3_analogy` | local | ✅ |
| C3 | 선행 개념 확인 | 중간 | `C3_prerequisite` | global | ✅ |
| C3 | 발화 속도 적절성 | 중간 | `C3_pace` | metric | ✅ |
| C4 예시·실습 연계 | 예시 적절성 | **높음** | `C4_example` | local | ✅ |
| C4 | 실습 연계 | **높음** | `C4_practice` | local | ✅ |
| C4 | **오류 대응 (C4 소속)** | 중간 | `C4_error` | local | ✅ |
| C5 수강생 상호작용 | 이해 확인 질문 | **높음** | `C5_check` | local | ✅ |
| C5 | 참여 유도 | **높음** | `C5_engage` | local | ✅ |
| C5 | 질문 응답·상호작용 | **높음** | `C5_answer` | local | ✅ |

---

## 📋 동작 검증 결과 — 단일 강의 end-to-end

단일 강의(2026-02-02 오전)를 3섹션 → 6청크로 처리해 전처리 ①②부터 리포트 ⑧까지 완주했다.

| 단계 | 산출물 | 상태 | 비고 |
|---|---|---|---|
| ①② 전처리 | `raw.jsonl`, `merged.jsonl` | ✅ | 22,756발화 → 2,350블록 (전체 30강의) |
| ③④⑤ 정제 | `clean.jsonl`(3), `chunks.jsonl`(6) | ✅ | 1강의 3섹션 · Upstage Solar/임베딩 |
| ⑥ 분석 | `analysis.jsonl`(18행) | ✅ | 체크리스트 18항목 LLM 채점 · ~16회 호출 |
| **⑦ 스코어링** | `scores.json` | **✅** | 종합 **36.1점** · LLM 없음 |
| ⑧ 리포트 | `report_2026-02-02_오전.md/pdf` | ✅ | MD(10KB) + PDF(115KB) |

**카테고리별 점수 (2026-02-02 오전, 3섹션 기준):**

| 카테고리 | 점수 | 주요 항목 |
|---|---|---|
| C1 언어 표현 품질 | **100.0** | 반복표현·완결성·일관성 모두 5점 |
| C2 강의 도입·구조 | 34.1 | 복습연계 4점, 목표안내 3점, 순서·강조·요약 1점 |
| C3 개념 설명 명확성 | 32.5 | 선행확인 4점, 속도 3점, 정의 2점, 비유 1점 |
| C4 예시·실습 연계 | 28.1 | 예시 3점, 실습 2점, 오류대응 1점 |
| C5 수강생 상호작용 | **0.0** | 전 항목 1점 → 원인 아래 §진단 참고 |
| **종합** | **36.1** | |

---

## 🔍 C5 0.0 진단

### 판정: **버그 아님 — 표본 한계 + 부정증거 처리 정책 문제**

환산 공식·가중치·항목 매핑은 모두 PDF와 일치한다. 문제는 `score=1`이 두 가지 상황을 구분하지 못한다는 점이다.

**C5 항목별 LLM 판정 내용:**

| item_key | score | evidence | n_candidates | negative_evidence | comment 요약 |
|---|---|---|---|---|---|
| `C5_check` | 1 | 0개 | 2개 | False | "발췌된 청크에서 이해 확인 질문이 전혀 나타나지 않음" |
| `C5_engage` | 1 | 0개 | 3개 | False | "발췌문 전체에서 직접 참여 유도 내용이 전혀 나타나지 않음" |
| `C5_answer` | 1 | 0개 | **0개** | **True** | "강의에서 해당 항목 관련 발화가 검색되지 않음(부정 증거)" |

**환산 추적:**

```
C5_check  : score=1 → norm=0.0% × w=3 = 0.0
C5_engage : score=1 → norm=0.0% × w=3 = 0.0
C5_answer : score=1 → norm=0.0% × w=3 = 0.0
C5 카테고리: 0.0 / 9.0 = 0.0   ← 공식 정상, 입력값이 문제
```

**원인 분석:**

- **구조적 표본 한계** — 3섹션은 강의 초반(Java I/O 개념 설명 구간)이다. 상호작용 발화는 강의 중반 이후에 등장하는 경향이 있어, 3섹션 표본에는 구조상 C5 발화가 없을 수 있다.
- **`C5_answer`의 `negative_evidence=True`** — 임베딩 검색 후보 자체가 0개(`n_candidates=0`). 이는 "5점 척도 최하"가 아니라 "발화 부재로 판단 불가"에 가깝다. 현재 `score=1`은 이 두 상황을 구분하지 않는다.
- **`C5_check`·`C5_engage`** — 후보(2~3개)가 있었으나 LLM이 "해당 없음" 판정. 강의 초반 구간 특성상 실제로 이해 확인·참여 유도 발화가 없었을 가능성이 높다.

**종합점수 영향:**

| 처리 방식 | 종합점수 |
|---|---|
| 현재 (C5 모두 score=1) | 36.1점 |
| C5 N/A 처리 시 | **45.1점** (+9.0점) |

> C5 3항목이 모두 **가중치 높음(3)**이라 처리 정책이 종합점수에 9점 이상 영향을 미친다. `negative_evidence` 기반 N/A 전환 여부가 핵심 설계 결정 사항이다.

---

## ⚙️ 설계 진단표

| 항목 | PDF 요구 | 현재 구현 | 진단 |
|---|---|---|---|
| **가중치** | 높음·중간·낮음 3단계 | `{high:3, mid:2, low:1}` · 항목가중 평균 | ✅ 일치 |
| **환산 공식** | 1~5점 → 0~100% 선형 | `(s-1)/(5-1)*100` | ✅ 일치 |
| **항목 매핑** | 5카테고리 18항목 | 18/18 일치 · C4_error C4 소속 정확 | ✅ 완전 일치 |
| **N/A 처리** | N/A 항목은 계산 제외 | `score=None` → 가중 제외 | ⚠️ 부분 불일치 — `negative_evidence=True`여도 `score=1`로 처리됨 |

---

## 🚧 고도화 후보 (미결정 · 추후 논의)

### 1. `negative_evidence=True` → N/A 자동 전환 (**최우선 검토**)

검색 후보 자체가 0개인 항목(`n_candidates=0` + `negative_evidence=True`)은 "해당 발화 없음"이므로 `score=1`(최하) 대신 `score=None`(N/A)이 더 적합하다.

단, **조건 분기가 필요**하다:
- **부분 분석일 때** (3섹션 등 표본 한정) → `negative_evidence=True`면 N/A 처리 (발화 부재 확정 불가)
- **전체 강의 분석 완료 후** → `negative_evidence=True`면 "실제 부재"로 간주해 score=1 유지 가능

이 분기를 위해 분석 시 전체 섹션 수 대비 처리 섹션 수를 `analysis.jsonl`에 기록하는 방안 검토.

### 2. 전체 30강의 실행 — C5 분포 확인

3섹션은 강의 초반 설명 구간이라 C5 발화가 구조상 나오지 않을 수 있다. 전체 실행으로 C5가 의미 있는 값을 가지는지 확인이 필요하다.

- **예상 비용** — 정제 564섹션(LLM ~564회 + 임베딩 ~564회) + 분석 ~480회(30강의 × 16건) — 실행 전 dry-run 비용 산정 권장
- 참고: readme_V2.md 검증 결과(2026-02-03 강의, 전체 95청크 기준)에서 강의형 오전 C5=8.3 vs 실습형 오후 C5=25.0으로 세션 유형별 차등이 확인됨. 단 이는 현재 jch 분석(2026-02-02 오전, 3섹션, C5=0.0)과 다른 강의·다른 범위의 결과임. 전체 30강의 실행 시 C5가 유의미한 분포를 가질 가능성을 시사함.

### 3. 표본 편향 경고 메타 기록

부분 분석 결과에 `"analyzed_sections": 3, "total_sections": X` 형태로 커버리지를 기록해, 리포트에서 "전체 대비 X섹션 기준" 경고 표시 추가 검토.

---

## 🐛 발견된 버그 및 수정 이력

| 파일 | 줄 | 문제 | 수정 |
|---|---|---|---|
| `scripts/run_refine_local.py:149` | manifest 기록 | `config.TAG_SIM_THRESHOLD` 없음 (`AttributeError`) | `config.TAG_RETRIEVE_FLOOR`로 교체 — `src/config.py` 상수명 변경 반영 |
