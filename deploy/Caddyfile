# Caddy — 도메인 + 자동 HTTPS(Let's Encrypt) + Streamlit 리버스프록시.
# DNS: lectureAnalzer.yeseulkim.cloud 의 A레코드를 이 인스턴스 공인 IP로.
lectureAnalzer.yeseulkim.cloud {
	encode zstd gzip
	# Streamlit 은 websocket 사용 — Caddy reverse_proxy 가 자동 업그레이드 처리
	reverse_proxy app:8503
}
